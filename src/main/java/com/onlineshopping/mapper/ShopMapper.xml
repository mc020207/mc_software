<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.onlineshopping.mapper.ShopMapper">

    <select id="selectShopsByRange" resultType="Shop">
        select * from `shop`
        where shopIsOpen = 2
        order by shopId
        limit ${startRow}, ${number}
    </select>

    <select id="selectShopsBySingleAttr" resultType="Shop">
        select * from `shop` where ${fieldName} = #{fieldValue} order by shopId
    </select>

</mapper>
